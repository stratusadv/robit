import unittest

from robit.job import enums


class TestEnums(unittest.TestCase):
    def setUp(self):
        self.enums = None

    def test_something(self):
        try:
            self.assertTrue(True)
        except:
            self.assertTrue(False)

